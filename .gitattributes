# 部署脚本 / systemd unit / nginx 配置必须强制 LF：
# Windows 上 git 的 autocrlf 会把它们转成 CRLF，传到 Linux 后 bash 会报
#   /bin/bash^M: bad interpreter: No such file or directory
deploy/** text eol=lf
Jenkinsfile text eol=lf
*.sh text eol=lf
*.service text eol=lf
*.conf text eol=lf
*.env text eol=lf
